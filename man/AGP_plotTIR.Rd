% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AGP_plots.R
\name{AGP_plotTIR}
\alias{AGP_plotTIR}
\title{Plot Time in Range (TIR) for CGM Data}
\usage{
AGP_plotTIR(data, ID, time, glucose, target_glucose)
}
\arguments{
\item{data}{A data frame containing CGM readings.}

\item{ID}{A character string specifying the column name representing the subject ID.}

\item{time}{A character string specifying the column name representing the timestamp of CGM readings.}

\item{glucose}{A character string specifying the column name representing glucose values.}

\item{target_glucose}{A numeric vector of length 2 specifying the lower and upper bounds for the target glucose range (e.g., `c(70, 180)`).}
}
\value{
A ggplot object representing a stacked bar chart of glucose distribution.
}
\description{
This function generates a stacked bar plot visualizing the distribution of glucose levels
into three categories: "Low", "Target", and "High". The percentage of time spent in
each range is computed for each subject.
}
\examples{
\dontrun{
# Example dataset
df <- data.frame(
  patient_id = rep(1:2, each = 100),
  date_time = rep(seq.POSIXt(from = Sys.time(), by = "5 min", length.out = 100), 2),
  Glucose = c(rnorm(100, mean = 120, sd = 30), rnorm(100, mean = 150, sd = 40))
)

# Plot TIR
AGP_plotTIR(df, ID = "patient_id", time = "date_time", glucose = "Glucose", target_glucose = c(70, 180))
}
}
